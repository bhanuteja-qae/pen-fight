# Pen-Fight Handoff — 2026-09-14

**Repo:** `~/pen-fight` (branch `main`) · remote `github.com/bhanuteja-qae/pen-fight`
**HEAD:** `3b60756` (clean, pushed) — *"Track .import sidecars for classroom floor textures"*
**Engine:** `~/godot/Godot_v4.7.2-stable_linux.x86_64` (4.7.2.stable.official.ed1daf0bf)
**Machine:** Ubuntu 26.04 x86_64, 2 cores, 7.7 GB RAM, ~19 GB disk. No `gh` CLI. `python3` (no pip module; PEP 668 — use venv/uv).
**Auth:** all GitHub via git/HTTPS + credential store (`~/.git-credentials`, `credential.helper=store`); OpenRouter key in `~/.hermes/.env` (line ~503 `GITHUB_TOKEN=`) + `~/.hermes/profiles/clutch/auth.json`. **Never reprint values; redact with `sed 's/=.*/=***/'`.**

---

## 1. Game state

Turn-based 2-player "knock the opponent's pen off the table" physics game, top-down classroom POV, Android target. Phase 1 (headless-feasible vertical slice + acceptance proxy) is complete. All 7 headless suites pass; live Xvfb runs at 0 script errors.

**Shipped since the last handoff-relevant boundary:**
- Review-council fixes, one pass (`c199254`): same-tick double-OOB tie rule (flicker loses) via deferred `resolve_pending_oob()`, 8s in-flight backstop, Amber/Cobalt naming everywhere, `TABLE_RECT` → 1180×640, 20-round gate balance floor (≥1 win/side), cancel radius 15→30px, idle "YOUR FLICK" turn cue (dashed oval + banner).
- Classroom floor (`8f0c993` + `3b60756`): the black void outside the table was the table *texture's* own bleed (1440×840 asset vs 1280×720 viewport), not the backdrop. Fixed at the source: `table_classroom.png` (desk kept pixel-identical, bleed repainted as light-gray tiled linoleum, 14px feathered edge) + `background_classroom.png` fallback behind the table for ultra-wide. Scene swapped both.

## 2. Key files

| Path | Role |
|---|---|
| `game/scenes/main.tscn` | 89-line spec-scale scene: classroom bg + CanvasModulate (0.75/0.74/0.92) + table 1180×640 + amber (−160,−80) / cobalt (160,80) pens rot 0 + ExitZone + TableBounds + Camera2D |
| `game/scripts/main.gd` | Orchestrator: phase machine, gate, ceremony, autoplay hook (`PENFIGHT_AUTOPLAY=1`), feel/debug/audio wiring |
| `game/scripts/pen_body.gd` | Capule physics: `MAX_IMPULSE 1600` (scaled inside `apply_flick`, human 0..1 power), settle 6.0/0.4, debounce 0.25, `MOVED_LINEAR_VEL 25.0` |
| `game/scripts/turn_state.gd` | Deferred OOB (`_oob_pending` + `resolve_pending_oob()`), 8s `_resolve_elapsed` backstop, stalemate forfeit, `decided_by_oob()` |
| `game/scripts/aim_input.gd` | Touch/mouse drag, cancel radius 30px, `input_locked` |
| `game/scripts/aim_overlay.gd` | Grab ring/cone/power/spin arc + idle turn cue (`show_turn`) |
| `game/scripts/turn_gate.gd` | Tap-to-continue CanvasLayer (dim + label), input-locks during handoff |
| `game/scripts/auto_flick.gd` | Deterministic harness: `auto_flick_requested(player, impulse, contact_offset)` (3-arg — test handlers must match) |
| `game/scripts/audio.gd` | One-shot pattern: stop → `clear_buffer()` → `push_buffer()` → `play(0.0)`; degrades to logged no-op on dummy driver |
| `game/scripts/feel.gd` | Trauma² camera roll (`MAX_ROLL_DEG 3.0`), hit-stop |
| `game/scripts/debug_overlay.gd` | Debug-only velocity overlay (`OS.is_debug_build()`) |

**Tests** (`game/tests/`): `turn_state_test` (incl. 4 new: tie rule, backstop, no-op resolve, continue cycles), `auto_flick_test`, `rounds_gate_test` (20-round seeded gate: `POWER_SEED=42`, power 0.5–1.0, balance floor), `torque_arm_probe`, `impulse_offset_probe`, `aim_overlay_smoke`.

**Docs:** `docs/ART_AND_FEEL_SPEC.md` (⚠️ *three phases stale*: still documents 360px pens / 1120×600 / CoM-OOB vs shipped 180px / 1180×640 / buffered-OOB — flagged for refresh), `docs/android-foundation.md` (current), `docs/RESEARCH.md`, phase contracts (`phase05/1a/1b/1d-contract.md`), `game/docs/turn_gate.md`.

## 3. How to run gates

```bash
# import (cwd MUST be game/; run twice; wipe .godot/exported after scene edits)
cd ~/pen-fight/game
DISPLAY=:99 LIBGL_ALWAYS_SOFTWARE=1 ~/godot/Godot_v4.7.2-stable_linux.x86_64 --headless --editor --quit --import .
# unit suites (any test):
DISPLAY=:99 LIBGL_ALWAYS_SOFTWARE=1 timeout 120 ~/godot/Godot_v4.7.2-stable_linux.x86_64 --headless --path . --script res://tests/<name>.gd
# live render (Xvfb :99 must be up):
DISPLAY=:99 LIBGL_ALWAYS_SOFTWARE=1 PENFIGHT_AUTOPLAY=1 ~/godot/Godot_v4.7.2-stable_linux.x86_64 --path .
ffmpeg -y -loglevel error -f x11grab -video_size 1280x720 -i :99 -frames:v 1 /tmp/frame.png
```

**Critical gotchas (learned the hard way):**
- **Stale `.godot/exported` cache:** after editing scene textures, the running game can keep serving the old compiled scene — delete `.godot/exported` + kill/relaunch the game process before trusting a recapture. Pixel-truth (corners of the frame) is the only reliable verification; the vision model misreads cool-tinted grays as "black" (flags amber as "red", floor as "void").
- `apply_impulse` position arg = **offset vector in body basis** (no translation) — probe-measured, `torque_arm_probe.gd` locks it.
- Human power fraction only — never pass raw magnitudes into `apply_flick` (double-scaling → 2.56M impulse eject).
- `PackedVector2Array()` — `PackedVector2Array.new()` is invalid in 4.7.
- `clear_buffer()` hard-fails on an *active* generator — stop first.
- Display names **Amber/Cobalt**, never Red/Blue (matches art, CVD-safe).

## 4. Android export (foundation done)

Artifacts in `~/pen-fight/build/`: `pen-fight.apk` (debug, 33.6 MB, arm64, v2-signed) · `pen-fight-debug.aab` · `pen-fight-release.aab` (release keystore at `build/secrets/penfight-release.keystore`, gitignored — **password [REDACTED], chmod 600, never commit**).

```bash
cd ~/pen-fight/game
DISPLAY=:99 LIBGL_ALWAYS_SOFTWARE=1 ~/godot/Godot_v4.7.2-stable_linux.x86_64 --headless --export-debug "Android" ../build/pen-fight.apk
```

Gotchas: hidden `game/android/.build_version` = `4.7.2.stable` is REQUIRED for gradle-built templates; `game/android/build/.gdignore` prevents `.import` sidecars from breaking gradle; verify AABs via `jarsigner -verify` (apksigner can't read AABs). `~` does NOT expand in `export_presets.cfg` — absolute paths only. If a fresh APK is requested after the classroom commits, rebuild before sending (stale artifact trap).

## 5. Open items / next moves

**Blocked on the user (the real Phase-1 gate):** human 20-round hot-seat playtest of the current APK. Questions: *want a 21st?* / *did one side feel unfairly favored?* (headless: blue leads 4–16 via winner-starts momentum, flagged as playtest question, NOT a harness bug) / *did the spin arc teach the corner flick?* Plus Play Console registration (starts the 14-day clock).

**Ready-to-run queue (approved direction, not yet dispatched):**
1. Touch-input synthetic-event test (`InputEventScreenTouch`/`ScreenDrag` → AimInput → TurnState → PenBody end-to-end) — the council's weakest coverage spot.
2. Full 3-cue turn system (mockup parity — code slice `show_turn` already proven live).
3. Coulomb friction behind a flag + per-skin mass lever (review item 9) — small, A/B-able.
4. Refresh `docs/ART_AND_FEEL_SPEC.md` to shipped geometry.

**Deferred council items (Security/DevX, low priority, no secrets risk):** `.claude/` + `.mcp.json` gitignore + history scrub (recoverable in object store, nothing secret ever committed) · keystore password hardening via env-var injection · float guards (zero-impulse reject, `half_len<0.0001` guard, fix misleading `!= Vector2.ZERO` comment) · input-layer test (covered by queue item 1).

**Housekeeping:** `smoke/` contains two stale `404: Not Found` artifacts (failed-fetch remnants; harmless, doc-accuracy only). Background delegation results from earlier waves (Phase 1a/1b) may arrive late as "async batch complete" messages — they are already integrated, reconcile-and-ignore.

## 6. Suggested skills for the next session

- `mattpocock:handoff` — this doc's format
- `software-development:godot-headless-vision-loop` — the capture/verify loop (main workflow)
- `software-development:godot-headless-android-export` — Android rebuild path
- `software-development:test-driven-development` — for queue item 1 (new input tests)
- `software-development:subagent-driven-development` — contract-gated agent waves for queue items 2–4
- `agent-council:review-code` — pre-commit review of the next wave
- `autonomous-ai-agents:hermes-agent` — if configuring Hermes itself

## 7. Credential map (all REDACTED by policy — do not reprint)

| Secret | Location |
|---|---|
| GitHub token (ghp_) | `~/.hermes/.env` line ~503 + `~/.git-credentials` (both chmod 600) |
| OpenRouter API key | same `.env` + `~/.hermes/profiles/clutch/auth.json` credential pool |
| Release keystore password | [REDACTED] — `build/secrets/penfight-release.keystore`, gitignored |
| Debug keystore password | [REDACTED] — in gitignored `game/export_presets.cfg` |