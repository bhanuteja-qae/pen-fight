# Godot engine cache (imported resources, editor state) — never commit
.godot/

# Android export artifacts (docs/RESEARCH.md §3.3: export_presets.cfg holds plaintext keystore passwords)
android/build/
game/android/
export_presets.cfg
*.keystore
build/

# Logs / OS noise
*.log
.DS_Store
# Asset tooling virtualenv (Pillow for generate_assets_*.py)
.asset-venv/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Prototype scratch: sweep JSONs and adversarial probes that DECISION.md cites
# as throwaway evidence (see docs/prototypes/app-flow/DECISION.md "not a
# deliverable"). Kept on disk for re-verification, never committed.
.scratch/
